import 'package:flutter/material.dart';

class CaregiverTaskScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Caregiver Task Management'),
      ),
      body: Center(
        child: Text('This is the Caregiver Task Management screen!'),
      ),
    );
  }
}
